type: object
properties:
  perkIds:
    type: array
    items:
      type: integer
      format: int64
    nullable: true
  arrivalDate:
    type: string
    format: date-time
  departureDate:
    type: string
    format: date-time
  originId:
    type: integer
    format: int64
  destinyId:
    type: integer
    format: int64
additionalProperties: false
